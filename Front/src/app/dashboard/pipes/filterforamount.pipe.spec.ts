import { FilterforamountPipe } from './filterforamount.pipe';

describe('FilterforamountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterforamountPipe();
    expect(pipe).toBeTruthy();
  });
});
